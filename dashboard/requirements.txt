# Streamlit Community Cloud installs THIS file — it picks the requirements
# nearest the main module (dashboard/app.py). This is the single deploy file.
# Local dev: see README quick start (ingest/ + this + requirements-dev.txt).
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
pyarrow>=14.0.0
# Ask page (qa/ pipeline): LLM client + schema/guard/spec deps
google-genai>=2.0.0
pydantic>=2.0.0
sqlglot>=25.0.0
pyyaml>=6.0
