version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: etf_prices
        description: Daily ETF OHLCV from ingest
        columns:
          - name: ticker
            tests: [not_null]
          - name: price_date
            tests: [not_null]
          - name: adj_close
            tests: [not_null]
